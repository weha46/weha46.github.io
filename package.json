{
  "devDependencies": {
    "tailwindcss": "^3.4.16"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./input.css -o ./style.css --watch"
  }
}